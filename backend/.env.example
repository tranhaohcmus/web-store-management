# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=H@o7dtta
DB_NAME=ecommerce_db
DB_DIALECT=mysql

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=1h
JWT_REFRESH_SECRET=your-refresh-token-secret-change-this-too
JWT_REFRESH_EXPIRE=7d

# Server Configuration
PORT=3000
NODE_ENV=development

# Upload Configuration (Optional - đã có trong middlewares/uploads/config)
UPLOAD_PATH=./public/upload
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Redis (Optional - for session/cache)
REDIS_HOST=localhost
REDIS_PORT=6379

# Email (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=haotranhcmus@gmail.com
SMTP_PASS=H@o7dtta

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
